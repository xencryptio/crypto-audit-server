version: '3.8'

services:
  crypto-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crypto-server
    ports:
      - "9000:8000"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - crypto-network

networks:
  crypto-network:
    driver: bridge
